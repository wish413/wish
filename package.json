{
  "name": "grunt-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "cmd": "0.0.4",
    "grunt": "^0.4.5",
    "grunt-cmd-concat": "*",
    "grunt-cmd-transport": "*",
    "grunt-contrib-clean": "*",
    "grunt-contrib-uglify": "^0.9.1"
  }
}
